spring:
  application:
    name : config-server
  profile:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations 

server:
  port: 8888
